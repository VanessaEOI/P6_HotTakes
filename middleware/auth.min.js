const jwt=require("jsonwebtoken");require("dotenv").config(),module.exports=(req,res,next)=>{try{const token=req.headers.authorization.split(" ")[1],decodedToken=jwt.verify(token,process.env.RANDOM_TOKEN_SECRET),userId=decodedToken.userId;if(req.body.userId&&req.body.userId!==userId)throw"Invalid user ID";next()}catch{res.status(401).json({error:new Error("Invalid request!")})}};
//# sourceMappingURL=auth.min.js.map